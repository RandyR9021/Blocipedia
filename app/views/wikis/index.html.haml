- @wikis.each do |wiki|
  %h2= link_to wiki.title, wiki_path(wiki)
  %p
    Published at
    = wiki.created_at.strftime('%b %d, %Y')
  %p= truncate(wiki.body, length: 200)
- if user_signed_in?
  = link_to "New Wiki", new_wiki_path
